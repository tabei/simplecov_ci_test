machine:
  timezone:
    Asia/Tokyo
  ruby:
    version: 2.3.1
test:
  pre:
    - bundle exec rspec
  post:
    - |
      curl -s -S -X POST --data-urlencode "payload={'channel': '#test_tabei', 'username': 'test-webhook', 'text': 'coverage report: <$https://circle-artifacts.com/gh/GitHub/tabei/simplecov_ci_test/$CIRCLE_BUILD_NUM/artifacts/0$CIRCLE_ARTIFACTS/coverage/index.html|open report :earth_asia: >' }" $SLACK_TEST_HOOK >/dev/null
